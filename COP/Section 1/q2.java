PATH
----

It is used for setting up the JDK binaries so that the system could recognise the command from the terminal.
In other words all commands need their corresponding binaries path to be set in PATH for them to be executed.