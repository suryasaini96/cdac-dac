final 
-----

Used for declaring a final value of that variable which should not be changed. Its like the const keyword of C. 

Access Modifiers
----------------

private -- Used for setting accessibility as ONLY within a class.
protected -- Used for setting accessibility as WITHIN a class and also it's SUB class.
public -- Used for setting accessibility from ANYWHERE within or outside the class.

Constructors
------------

Used for constructing an object of a class. When none is provided compiler inserts a default constructor by itself. Can we used for setting up the values of member variables of a class. 
